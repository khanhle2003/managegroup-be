server:
  port: 8080
  servlet:
    context-path: 



    
spring:
  datasource: 
    url: "jdbc:mysql://localhost:3306/managegroup"
    # url: "jdbc:mysql://localhost:3306/new_schema"
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: mysql
    database-platform: org.hibernate.dialect.MySQL8Dialect