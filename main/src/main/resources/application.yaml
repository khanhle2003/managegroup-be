server:
  port: 8080
  servlet:
    context-path: /main



    
spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/new_schema"
    username: root
    password: Khoa@2003
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: mysql
    database-platform: org.hibernate.dialect.MySQL8Dialect